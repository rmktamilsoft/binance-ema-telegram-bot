      - name: Check Runner Network
        run: |
          echo "Public IP:"
          curl -s https://api.ipify.org
          echo
          echo "IP details:"
          curl -s https://ipinfo.io/json
          echo
